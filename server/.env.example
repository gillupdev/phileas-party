# Google OAuth Credentials
# Get these from https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-client-id-here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret-here

# Session secret (generate a random string for production)
SESSION_SECRET=your-random-session-secret-here

# Callback URL - ONLY set this in production!
# Leave unset for local development (will auto-resolve to localhost:3000)
# CALLBACK_URL=https://your-app.koyeb.app/api/auth/google/callback

# ========================================
# Google Cloud Console - Authorized redirect URIs
# Add BOTH of these to your OAuth credentials:
#   - http://localhost:3000/api/auth/google/callback (for local dev)
#   - https://your-app.koyeb.app/api/auth/google/callback (for production)
# ========================================
